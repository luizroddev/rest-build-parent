name: CI Build

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    uses: ./.github/workflows/ci-build.yml
    with:
      java-version: '22'
    secrets:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
